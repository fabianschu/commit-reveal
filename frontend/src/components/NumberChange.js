import React, { useContext } from "react";
import { Web3Context } from "../context/Web3Context";

const NumberChange = () => {
  // const { increaseBy, decreaseBy } = useContext(Web3Context);

  return (
    <>
      {/* <button onClick={() => increaseBy(100)}>Increase by 100</button>
      <button onClick={() => decreaseBy(100)}>Decrease by 100</button> */}
    </>
  );
};

export default NumberChange;
